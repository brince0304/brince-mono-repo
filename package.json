{
	"name": "brince-blog-mono",
	"version": "1.0.0",
	"main": "index.ts",
	"scripts": {
		"dev:blog": "pnpm --filter @brince-mono-repo/blog run dev",
		"build:blog": "pnpm --filter @brince-mono-repo/blog run build",
		"start:blog": "pnpm --filter @brince-mono-repo/blog run start",
		"build:components": "pnpm --filter @brince-mono-repo/shared-components run build",
		"build:styles": "pnpm --filter @brince-mono-repo/shared-styles run build",
		"build:all": "pnpm run build:components && pnpm run build:styles && pnpm run build:blog",
		"storybook:components": "pnpm --filter @brince-mono-repo/shared-components run storybook",
		"build-storybook:components": "pnpm --filter @brince-mono-repo/shared-components run build-storybook",
		"test": "echo \"Error: no test specified\" && exit 1",
		"lint": "biome lint .",
		"format": "biome format . --write"
	},
	"dependencies": {
		"next": "14.2.5",
		"next-themes": "^0.3.0",
		"react": "^18",
		"react-dom": "^18",
		"framer-motion": "^11.3.28"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"eslint": "^8.8.0",
		"@types/node": "^20",
		"@types/react": "^18",
		"@types/react-dom": "^18",
		"storybook": "^8.2.9",
		"typescript": "^5",
		"tailwindcss": "^3.4.1",
		"rollup": "^2.60.0"
	},
	"private": true,
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": ""
}
