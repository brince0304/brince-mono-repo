{
	"version": 2,
	"buildCommand": "pnpm install && pnpm -w run build:all",
	"outputDirectory": "apps/blog/.next",
	"framework": "nextjs",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "apps/blog/$1"
		}
	]
}
